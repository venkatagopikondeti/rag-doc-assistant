# Kubernetes

Kubernetes schedules containers onto a cluster of nodes and keeps the
declared number of replicas running. A Deployment manages ReplicaSets and
supports rolling updates with automatic rollback when readiness probes fail.

Horizontal Pod Autoscaling adjusts replica counts based on observed CPU or
custom metrics.
