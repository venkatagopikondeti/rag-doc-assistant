# MLflow

MLflow is an open source platform for managing the machine learning lifecycle.
It has four components: Tracking, Projects, Models and the Model Registry.

Tracking records parameters, metrics and artifacts for every run, so two
experiments can be compared after the fact. The Model Registry adds stages
such as Staging and Production, plus lineage back to the run that produced
the model version.
